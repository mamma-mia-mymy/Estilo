name: estilo
description: "Estilo - Digital Wardrobe Organizer and Outfit Evaluation Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.1
  image_picker: ^1.0.0
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  firebase_storage: ^12.0.0
  cloud_firestore: ^5.0.0
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  http: ^1.2.0
  geolocator: ^13.0.0
  geocoding: ^3.0.0
  cached_network_image: ^3.4.1
  permission_handler: ^11.3.0
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
